- file:
    state: "directory"
    path: "{{ item }}"
  with_items:
    - "{{ data_path }}/"
    - "{{ data_path }}/data"
    - "{{ data_path }}/saved_caches"
    - "{{ data_path }}/commitlog"
    - "{{ data_path }}/spark"